foods = ['Butter', 'tuna', 'burger', 'snacks', 'pizza']

# Takes the list and deal with each item individualy
# For Each item in foods access it & named f
for f in foods:
    print(f)
    print(len(f))

# to Slice the list the upto particular index
# for f in foods[:2]
