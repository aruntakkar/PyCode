"""
 Methods that use dot notation only works with strings Such as
 "string".upper()
 on the other hand len(string) and str(object) can work on other data types.
"""

about = "We're good company"

# Calculating the length of about string
print(len(about))

# Converting to Upper string
print(about.upper())
